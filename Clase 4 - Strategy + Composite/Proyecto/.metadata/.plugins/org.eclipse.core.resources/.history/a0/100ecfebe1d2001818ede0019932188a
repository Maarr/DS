package datos;

import formulario.Campo;

public class Sueldo extends DatoFormulario {
	
	public Sueldo() {
		super(new Campo("Sueldo"));
	}

	@Override
	public boolean validar() {
		return (super.getValidador().getNumeros().validarSoloDigitos(super.getDato()) &&
				super.getValidador().getMayorQue().validarMayorQue(super.getDato(), 800) &&
				super.getValidador().getMenorQue().validarMenorQue(super.getDato(), 1200));
	}

}
